# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.57.1  # Qwen-Image-2512 model config requires text_config support
accelerate>=0.24.0
safetensors>=0.4.0

# Install diffusers from git for latest features
# Run: pip install git+https://github.com/huggingface/diffusers
# Or use: diffusers @ git+https://github.com/huggingface/diffusers
diffusers @ git+https://github.com/huggingface/diffusers

# Quantization support
bitsandbytes>=0.41.0

# LoRA support
peft>=0.13.0  # Updated - compatible with transformers 4.46+

# Image processing
pillow>=10.0.0
opencv-python>=4.8.0

# Web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# API clients and utilities
requests>=2.31.0
aiofiles>=23.2.0
httpx>=0.25.0

# Optional but recommended
scipy>=1.11.0
numpy>=1.24.0
tqdm>=4.66.0
huggingface-hub>=0.19.0

# For CUDA support (optional, comment out if CPU-only)
# cuda-python>=12.0.0

# Development tools (optional)
# pytest>=7.4.0
# black>=23.0.0
# isort>=5.12.0